server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: nexusone-ai

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/nexusone_ai?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Aqzpgv@2013}
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update

    open-in-view: false
    show-sql: false

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

springdoc:
  swagger-ui:
    path: /swagger-ui.html

nexusone:
  repositories:
    path: C:/NexusOne-Sample-Applications

  github:
    token: ${GITHUB_TOKEN:}
    api-url: https://api.github.com
  jenkins:
    base-url: ${JENKINS_BASE_URL:http://localhost:8181}
    username: gundusanthosh
    api-token: 11d23f47ce5653e1eaca4e391872f53429
    connect-timeout-seconds: 10
    request-timeout-seconds: 30